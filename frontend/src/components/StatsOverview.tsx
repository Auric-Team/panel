"use client";

import React from 'react';
import { Overview, OverviewProps } from './Overview';

export const StatsOverview: React.FC<OverviewProps> = (props) => {
  return <Overview {...props} />;
};
